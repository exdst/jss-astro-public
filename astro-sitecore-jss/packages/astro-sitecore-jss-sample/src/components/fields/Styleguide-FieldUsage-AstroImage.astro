---
import { AstroImage } from '@astro-sitecore-jss/astro-sitecore-jss';
import StyleguideSpecimen from '../styleguide/Styleguide-Specimen.astro';

export interface Props {
  route: any;
}

const { route } = Astro.props;
---

<StyleguideSpecimen route={route} e2eId="styleguide-fieldusage-astro-image">
  <p>Plain image</p>
  <AstroImage field={route.fields.sample1} height={51} width={204} />

  <p>Advanced image</p>
  <!--
    You can use any of the props available in the astrojs/image.
    E.g. we have used 'aspectRatio' to demonstrate how an image size could be calculated based on one dimension only(height) and an aspect ratio .
    imageParams: parameters that are passed to Sitecore to perform server-side resizing of the image
        Sample rescales image to max 100x50 dimensions on the server
        IMPORTANT: imageParams must be whitelisted for resizing to occur. See /sitecore/config/*.config (search for 'allowedMediaParams')
    other-attributes-pass-through
  -->
  <AstroImage
    field={route.fields.sample2}
    height={50}
    imageParams={{ mw: 100, mh: 50 }}
    aspectRatio={4 / 2}
    data-sample="other-attributes-pass-through"
  />

  <p>Responsive image</p>
  <AstroImage field={route.fields.sample2} fit={'fill'} />
</StyleguideSpecimen>
